class KeyValue {

  /**
   * Constructor
   * @param {string} key
   * @param {any} value
   */
  constructor(key, value) {
    this.key = key;
    this.value =  value;
  }
}

module.exports = KeyValue;
